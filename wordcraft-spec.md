# 词匠 WordCraft — 每日单词弹窗记忆软件 开发规格说明

版本: v0.1 (2026-08) · 平台: Windows 10/11 · 交付方式: Claude Code 实施

---

## 1. 问题与目标

**问题**: 目标用户（美术高中生，暑期在家，长时间使用电脑玩游戏）没有主动背单词的意愿。传统背单词 App 需要主动打开、单次时间长、审美说教，均不可行。

**产品定位**: 一个常驻他电脑的"游戏化单词伴侣"，每天在早/中/晚三个时段自动弹出极短的单词训练，通过掌握度模型自动识别会与不会，集中火力训练未掌握的词，并按遗忘曲线安排重复复习。视觉与玩法融合他喜欢的元素（像素风、二次元、赛车、收集），使其感受上更接近一个小游戏而非学习软件。

**目标**:
1. 用户每天在无强制的前提下完成 3 次弹窗训练（≥15 词次），连续使用 ≥21 天
2. 系统能对每个词给出会/不会/掌握中的判定，错词 100% 进入强化队列并在 24h 内重现
3. 一个学年内覆盖中考 1600 + 高考 3500 词库的首轮学习
4. 单次弹窗完成时间 ≤120 秒

**非目标**:
- 不做手机端、不做多用户/云同步（单机单用户，本地数据）
- 不做防卸载、防杀进程（放弃成本设计为心理性的断签，而非技术对抗）
- 不做语法/阅读/听力训练，只做词汇
- 不在用户界面中出现任何"家长监督"痕迹（周报走静默邮件）
- 不使用任何受版权保护的游戏素材（原神/我的世界的角色、贴图、音乐等），仅做风格借鉴与原创素材

---

## 2. 用户与核心场景

- 用户: 15 岁美术高中生，长时间在 Windows 电脑上玩游戏（Minecraft、原神、赛车类）、看二次元内容。对视觉品质敏感，对说教反感。
- 场景 A: 上午 10:00，他刚退出游戏回到桌面，弹窗出现，90 秒做完 5 个词，获得连击奖励和一次抽卡，窗口自动消失。
- 场景 B: 他正在打原神深渊（全屏），到达弹窗时段，程序检测到全屏，静默等待，他退出全屏后 1 分钟弹出。
- 场景 C: 昨天错的 3 个词，今天在三个时段中被优先安排重考，全部答对后从强化队列降级。
- 场景 D: 他主动打开主界面刷错词本、看自己的赛道排名和收集图鉴。

---

## 3. 功能需求

### P0（MVP 不可缺）

**F1 弹窗调度器**
- 三个每日时段（默认 9:00–11:00 / 13:00–15:00 / 19:00–21:00，用户可改），每时段弹一次
- 时段内每 5 分钟检测一次前台状态，使用 `SHQueryUserNotificationState`：处于 `QUNS_RUNNING_D3D_FULL_SCREEN` / `QUNS_BUSY` / `QUNS_PRESENTATION_MODE` 时不弹，退出全屏后延迟 60 秒弹出
- 弹窗为无边框置顶小窗（约 360×480），出现在屏幕右下角，不抢占键盘焦点，需用户点击后才进入答题
- "稍后 15 分钟"按钮，每时段最多推迟 3 次；时段结束仍未完成则并入下一时段（一次弹窗最多合并为 8 词）
- 验收: 全屏游戏运行期间 0 次弹出；非全屏状态下时段内必弹；连续推迟 3 次后第 4 次不可推迟但仍可手动关闭（计为当日未完成该时段）

**F2 掌握度模型（识别会/不会）**
- 调度算法采用 **FSRS**（用现成实现，如 `ts-fsrs`），每词维护 difficulty / stability / due date
- **不采用 Anki 式用户自评**。评级由系统根据以下信号自动映射为 FSRS rating：
  - 答对/答错
  - 反应时间（<3s 秒答 → Easy；3–8s → Good；>8s 答对 → Hard；答错 → Again）
  - 当前题型难度（见 F3，高阶题型答对权重更高）
- 每词状态机: `新词 → 学习中 → 强化中(答错进入) → 复习中 → 已掌握 → (抽查失败回退至强化中)`
- **已掌握判定**: stability > 60 天 且 最近一次高阶题型（例句挖空或拼写）通过；已掌握词仍以低频（约每 60–90 天）抽查
- **强化队列（未掌握词加强训练）**:
  - 答错的词当场重排到本次 session 队尾再考一次
  - 当天最后一个时段强制重现
  - 次日必现；连续 3 次在 8 秒内答对方可离开强化队列
  - 强化中的词在每次弹窗中占比不低于 40%（有则必排）
- 验收: 任意词答错后，24 小时内至少重现 2 次；连续答对 3 次前不得离开强化队列

**F3 题型体系（随掌握度升级）**
1. 英→中 四选一（新词默认）
2. 中→英 四选一
3. 听音辨词（播放发音，四选一）
4. 例句挖空（例句中该词挖空，四选一）
5. 全拼写（键盘输入，给中文释义与首字母提示）
- 词的状态每升一级，题型阶梯上移；强化中的词退回低阶题型重建
- 干扰项从形近/义近词中生成（编辑距离 + 同词性）
- 验收: 同一词在"学习中"与"复习中"阶段呈现的题型不同；干扰项无重复、无正确答案语义重叠

**F4 词库与发音**
- 内置: 中考 1600 词 + 高考 3500 词（去重合并，标注学段），词条含: 单词、音标、词性、释义、2 条例句、词频分层
- 附加词包: 美术/设计专业词汇（约 300 词: sketch, perspective, palette, render, composition…），艺考后置阶段实用
- 例句语境优先选用游戏/动漫/绘画相关的自然例句（自建或改写，避免课本腔）
- 发音: 首选 Edge-TTS 批量离线生成并缓存本地 mp3；离线兜底用 Windows SAPI
- 验收: 全部词条有释义与至少 1 条例句；发音点击后 300ms 内播放

**F5 摸底测试（入库分级）**
- 首次启动运行自适应摸底: 按词频分 5 层，每层抽样测试（英→中 + 反应时间），估计词汇量边界
- 判定为"已会"的词直接赋高 stability 进入低频复习池，不占用每日新词额度
- 全程做成"新手战力测试"的游戏化包装，约 5 分钟，可分两次完成
- 验收: 摸底后系统给出词汇量估计值；已会词当月不出现在新词队列

**F6 基础游戏化反馈**
- 连续天数（streak，三时段全完成计 1 天）、XP、等级、连击倍率（连续答对递增）
- 答对/答错/升级均有即时音效与轻动画（juicy feedback，可静音）
- 断签规则: 当日未完成三时段 → streak 清零（保留一次/月的"补签卡"，用赛道积分兑换）
- 验收: 从答题到反馈动画出现 <100ms

**F7 系统集成**
- 开机自启（注册表 Run 键，可在设置中关闭）、系统托盘常驻（图标显示今日进度 0–3）
- 托盘菜单: 立即练一组 / 打开主界面 / 今日暂停（每月限用 2 次）/ 设置
- 本地 SQLite 存储全部数据；支持数据导出 JSON

### P1（V1 完善）

**F8 主界面（不只是弹窗）**
- 仪表盘: 词汇量估计曲线、掌握分布（新/学习/强化/复习/已掌握五段色条）、日历热力图
- 错词本: 强化队列全列表，可主动刷
- 自由练习: 不限量刷词、拼写专项、听写模式
- 词库浏览与搜索
- 设置: 时段、每日新词量（默认 6，复习量自动）、主题皮肤、音量、邮件配置

**F9 赛季赛道（赛车元素）**
- 每周一个赛季，一条像素风赛道；每完成一个时段，赛车前进一段
- 对手为"幽灵车"= 上周的自己（无社交、无真人对比）
- 周末结算发放赛道积分，用于兑换抽卡券/补签卡/皮肤
- 验收: 赛道进度与实际完成次数一致；断签不清赛道积分（只清 streak）

**F10 收集系统（抽卡）**
- 每完成一个时段获得 1 张抽卡券；卡池两类:
  - **世界名画像素化卡牌**（公有领域画作: 星月夜、神奈川冲浪里、蒙娜丽莎…，贴合美术生身份，可做成"名画图鉴"含一句画作冷知识）
  - 原创像素生物卡（风格化原创，非任何游戏现有角色）
- 稀有度三档，重复卡自动转积分；图鉴页可查看收集进度
- 验收: 卡面素材全部为公有领域或原创生成，仓库内附素材来源清单

**F11 周报邮件（静默）**
- 每周日晚通过 SMTP 向家长邮箱发送: 完成率、新学/巩固词数、词汇量估计变化、最顽固的 10 个词
- 客户端界面内不出现任何与此相关的文案或入口（仅安装时配置文件中设置）

### P2（后续预留）

- 看板娘: 原创二次元陪伴角色（AI 生成立绘，多表情，答题反馈时出现），预留素材插槽
- 皮肤系统: 像素矿洞 / 二次元 / 车库 三套主题（P0 先做暗色默认主题，但样式层用 CSS 变量预留换肤）
- 自定义词包导入（CSV）
- 长期数据: 艾宾浩斯留存率报表

---

## 4. 视觉与风格要求

- 整体: **暗色、像素风 UI 基调**（8-bit/16-bit 质感的图标与进度条），二次元感体现在配色与角色插画上，禁止一切"教辅感"元素（如卡通铅笔、黑板、红勾作业本）
- 字体: 中文用思源黑体，数字/英文可用像素风字体（仅装饰位，正文保证可读性 ≥14px）
- 动效: 答对时的连击弹跳、抽卡开包动画、赛道推进动画，节制但要"爽"
- 素材策略: 所有角色/图标/音效为原创生成或 CC0/公有领域来源，**明确禁止使用原神、我的世界及任何商业游戏的角色名、立绘、贴图、音乐**；风格致敬（像素方块感、元素属性配色）不受此限
- 文案语气: 简短、玩家化（"完成任务 +50 XP"），不出现"加油学习""你要努力"类说教文案

---

## 5. 技术方案

- **框架**: Tauri 2（Rust 后端 + WebView2 前端），安装包小、内存占用低，适合常驻
- **前端**: React + TypeScript + Vite；样式用 CSS 变量支持换肤
- **算法**: `ts-fsrs`（或 Rust 侧 `fsrs-rs`），参数用默认权重起步，数据量够后可本地优化
- **存储**: SQLite（`tauri-plugin-sql`），表: words / word_states / review_logs / sessions / seasons / cards / settings
- **Rust 侧职责**: 全屏检测（`SHQueryUserNotificationState`）、托盘、开机自启、定时调度、SMTP 周报、TTS 缓存管理
- **弹窗**: 独立 Tauri 窗口，`always_on_top` + 无边框 + 不抢焦点（`focus(false)` 初始），点击进入后才捕获键盘
- 注: 本项目为单机桌面应用，不使用团队常规的 Docker/PostgreSQL 栈，数据层为本地 SQLite

## 6. 工程规范

- 按团队 Claude Code 项目惯例执行: 仓库根目录提供 `CLAUDE.md`（架构说明、命令、约定）、常用 slash commands、每个里程碑的 Definition of Done
- DoD 基线: 功能通过验收标准、`cargo clippy` / `eslint` 零警告、核心逻辑（FSRS 映射、状态机、调度器）有单元测试、可打包出可安装的 `.msi`
- review_logs 记录每次作答的完整信号（词、题型、正误、反应时间、rating），保证算法可回溯调参

## 7. 里程碑

- **M1 MVP（先跑起来）**: F1 调度+全屏检测、F2 掌握度模型、F3 题型 1/2、F4 词库+TTS、F6 基础反馈、F7 托盘自启 → 可安装试用
- **M2 完整体验**: F5 摸底、F3 题型 3/4/5、F8 主界面、错词本
- **M3 游戏化拉满**: F9 赛季赛道、F10 抽卡收集、F11 周报、音效动效打磨
- 建议 M1 完成后先装机观察一周留存，再决定 M2/M3 顺序微调

## 8. 开放问题（开工前确认）

1. 蜗牛电脑是否常联网？（影响 Edge-TTS 与周报邮件；均有离线/延迟兜底但需确认）
2. 每日新词量默认值: 建议 6 新词 + 复习自动（总词次约 15–20），是否符合预期？
3. 名画卡牌图源: 建议 WikiArt/Wikimedia 公有领域图 + 本地像素化处理脚本，是否接受？
4. 周报收件邮箱与 SMTP 发件配置由谁提供？
5. "今日暂停"月配额 2 次是否合适（外出/生病场景）？
