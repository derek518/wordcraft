# macOS
.DS_Store

# Node
node_modules/
dist/
*.local

# Rust / Tauri
target/
gen/schemas/

# Logs
*.log
npm-debug.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# 词库中间产物（原始素材不入库，仅提交清洗后结果与来源清单）
scripts/wordlist/raw/
scripts/cards/raw/

# 词库原始数据（63MB，不入库，获取方式见 scripts/wordlist/SOURCES.md）
scripts/wordlist/raw/
__pycache__/

# 密钥文件，禁止入库
.env
.env.local

# 构建产物：Python venv 与预生成音频
.venv/
# 3657 个 mp3 约 38MB。二进制文件会让仓库永久膨胀，且每次重新生成
# 都产生全新 blob。音频是构建产物，由 scripts/tts/pregenerate.py 产出，
# 打包前执行即可
wordcraft/public/audio/
